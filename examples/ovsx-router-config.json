{
  "registries": {
    "public": "https://open-vsx.org/",
    "mock": "${self}/mock-open-vsx/"
  },
  "use": [
    "mock",
    "public"
  ],
  "filters": {
    "requests": [
      {
        "ifRequestContains": "\\bsample-namespace\\b",
        "use": "mock"
      }
    ]
  }
}
